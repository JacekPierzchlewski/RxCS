RXCS toolbox, cs\ - directory with compressed sensing modules

contents:

    1. dict\          - directory with dictionaries

    2. L1\            - directory with L1-based reconstruction modules 

    3. auxiliary      - directory with auxiliary modules

    4. CONTENTS.txt   - the current file

    5. Makefile       - local Makefile

    6. __init__.py    - module initialization file

    7. @cvxoptL1.py   - link to module with L1-based signal reconstruction (regularized
                        regression), the optimization uses cvxopt
